import { DigitalTimerPipePipe } from './digital-timer-pipe.pipe';

describe('DigitalTimerPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DigitalTimerPipePipe();
    expect(pipe).toBeTruthy();
  });
});
